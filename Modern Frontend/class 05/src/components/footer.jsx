import React from 'react'

const Footer = ({ data, age }) => {
    // console.log("props", props.data)

    // const { data, age } = props
    // console.log("data, age" , data, age)
    return (
        <div>
            <h1>Footer</h1>
        </div>
    )
}

export default Footer
